nav.navbar.navbar-expand-lg.navbar-dark.bg-dark.fixed-top
    .container
        a.navbar-brand(href="/") SHOPPİNG
        button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
            span.navbar-toggler-icon
        .collapse.navbar-collapse#navbarNav
            ul.navbar-nav.ms-auto
                li.nav-item
                    a.nav-link(href="/", class=(path === "/" ? "active" : "")) Home
                li.nav-item
                    a.nav-link(href="/products", class=(path === "/products" ? "active" : "")) Products
                if isAuthenticated
                    li.nav-item
                        a.nav-link(href="/cart", class=(path === "/cart" ? "active" : "")) Cart
                    li.nav-item
                        a.nav-link(href="/orders", class=(path === "/orders" ? "active" : "")) Orders
                if isAdmin
                    li.nav-item
                        a.nav-link(href="/admin/products", class=(path === "/admin/products" ? "active" : "")) Admin Product
                    li.nav-item
                        a.nav-link(href="/admin/add-product", class=(path === "/admin/add-product" ? "active" : "")) Add Product
                    li.nav-item
                        a.nav-link(href="/admin/categories", class=(path === "/admin/categories" ? "active" : "")) Categories
                ul.navbar-nav.ml-auto
                    if isAuthenticated
                        li.nav-item
                            a.nav-link(href="/account/logout") Logout
                    else
                        li.nav-item
                            a.nav-link(href="/account/login") Login
                        li.nav-item
                            a.nav-link(href="/account/register") Register

// CSS Styles for Improvement
style.
    .navbar {
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .navbar-dark.bg-dark {
        background-color: #343a40 !important;
    }

    .navbar-brand {
        font-weight: bold;
        color: #f8f9fa;
        letter-spacing: 2px;
        text-transform: uppercase;
    }

    .navbar-nav .nav-link {
        color: #f8f9fa !important;
        font-size: 1.1rem;
        margin-left: 20px;
        text-transform: capitalize;
        padding: 10px 15px;
        border-radius: 25px;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .navbar-nav .nav-link:hover {
        background-color: #ff6f61;
        color: white;
    }

    .navbar-nav .nav-item.active .nav-link {
        background-color: #ff6f61 !important;
        color: white;
    }

    .navbar-toggler-icon {
        background-color: #ff6f61;
    }

    .navbar-collapse {
        justify-content: flex-end;
    }
